# LG-S42-001: Dirty Working Tree (~58 Modified Files)

**Severity:** MEDIUM
**Status:** OPEN
**Session:** 42
**Date:** 2026-03-31

## Description

The working tree on Oracle contains approximately 58 modified files from prior sessions (S39-S40).
These were not included in PR #75 or PR #77 and remain uncommitted.

## Impact

- `git status` is noisy, making it hard to see new changes
- Risk of accidentally staging stale changes in future commits
- Robin's git tasks may pick up unintended files

## Root Cause

Sessions 39-40 accumulated changes that were not fully committed before handoff.
PR #75 (S41) landed the backlog but did not cover all modified files.

## Recommended Fix

1. Run `git diff --stat` to inventory all modified files
2. Triage into categories: keep, discard, or defer
3. Either commit meaningful changes or `git checkout` stale ones
4. Consider adding problematic paths to `.gitignore` if they are generated artifacts

## Workaround

None active. Changes persist in working tree but do not affect CI (only committed code is tested).

## Lucius S52 Triage
Confirmed OPEN: Dirty working tree. Delegate to Robin for git triage.
