# Rudy Workhorse — Core Dependencies
# pip install -r requirements.txt --break-system-packages

# === Core (always needed) ===
requests>=2.31
beautifulsoup4>=4.12
pyyaml>=6.0
pydantic>=2.0
httpx>=0.25
psutil>=5.9
rich>=13.0
structlog>=23.0
jinja2>=3.1
arrow>=1.3
tabulate>=0.9
python-dotenv>=1.0
humanize>=4.0
tqdm>=4.65
colorama>=0.4
click>=8.0
typer>=0.9
jsonschema>=4.17
tenacity>=8.2
cachetools>=5.3
schedule>=1.2
watchdog>=3.0
cryptography>=41.0

# === Data ===
pandas>=2.0
openpyxl>=3.1

# === Web Intelligence ===
feedparser>=6.0
markdownify>=0.11
trafilatura>=1.6
html2text>=2024.2
tldextract>=5.0
python-whois>=0.9
dnspython>=2.4

# === NLP ===
textblob>=0.18
rapidfuzz>=3.5
chardet>=5.2
ftfy>=6.1

# === Financial ===
yfinance>=0.2.30

# === Email ===
# (uses stdlib imaplib/smtplib — no extra deps)

# === Voice / Audio ===
gTTS>=2.5
pydub>=0.25
soundfile>=0.12
yt-dlp>=2024.1

# === OCR / Docs ===
pdfplumber>=0.10
Pillow>=10.0
python-docx>=1.1
python-pptx>=0.6

# === API Server ===
fastapi>=0.100
uvicorn>=0.23

# === AI / ML (optional — larger installs) ===
# sentence-transformers>=2.2  # for knowledge_base.py
# chromadb>=0.4               # for knowledge_base.py
# langchain>=0.1              # for agent chains
# torch                       # for voice_clone, avatar (CPU)

# === Networking (optional) ===
# scapy>=2.5                  # for network_defense.py
# python-nmap>=0.7            # for presence.py

# === Dev Tools ===
ruff>=0.1
black>=23.0
